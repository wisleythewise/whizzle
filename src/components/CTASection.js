// CTASection.js
import React , { useEffect }  from 'react';

const CTASection = (props) => {
  

  return (        
  <section className="cta-section">
      <h2 className="headline">Start taking control of your inbox</h2>
      <p className="subheadline">Try it now and only receive the content you want!</p>
      <button className="cta-button">Try it now</button>
  </section>
  );
};

export default CTASection;